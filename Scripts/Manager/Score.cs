// Неизменяемый класс для получения и передачи очков, вызывается в скрипте GameManager и ScoreDisplay.
public static class Score
{   
    // Получаем и передаем набранное кол-во очков .  
    public static int score { get; set; }
}

